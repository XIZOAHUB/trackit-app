# Cloudflare Pages — Redirect Rules
# Ensures all routes serve index.html (SPA support)
# Also handles trailing slashes and .html extensions

# Redirect /index.html → / (clean URL)
/ / 301

# All other paths → serve index.html (PWA fallback)
/* / 200
